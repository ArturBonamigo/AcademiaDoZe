<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AcademiaDoZe.Presentation.AppMaui.Views.MatriculaListPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Matrículas">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <HorizontalStackLayout Spacing="8" Grid.Row="0">
            <SearchBar Placeholder="Buscar por aluno, CPF ou objetivo"
                       Text="{Binding SearchText}"
                       SearchCommand="{Binding SearchCommand}"
                       HorizontalOptions="FillAndExpand"/>
            <Button Text="+" Command="{Binding AddCommand}" WidthRequest="44" HeightRequest="44" CornerRadius="22"/>
        </HorizontalStackLayout>

        <CollectionView ItemsSource="{Binding Matriculas}" Grid.Row="1"
                        SelectionMode="Single"
                        SelectionChangedCommand="{Binding EditCommand}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="6" CornerRadius="8" HasShadow="False" BorderColor="#666">
                        <Grid ColumnDefinitions="Auto, *" Padding="8">
                            <StackLayout Grid.Column="1" Padding="8,0,0,0">
                                <Label Text="{Binding AlunoMatricula.Nome}" FontAttributes="Bold"/>
                                <Label Text="{Binding Plano}" FontSize="12"/>
                                <Label Text="{Binding DataInicio, StringFormat='Início: {0:dd/MM/yyyy}'}" FontSize="12"/>
                                <Label Text="{Binding DataFim, StringFormat='Fim: {0:dd/MM/yyyy}'}" FontSize="12"/>
                                <Label Text="{Binding Objetivo}" FontSize="12" LineBreakMode="TailTruncation"/>
                            </StackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
